const { resolve } = require('path') //Auxilia a navegação entre as pastas independente do sistema utilizado.

module.exports = {
    config: resolve( __dirname, 'src', 'config', 'database.js'), //Indicando o local dos arquivos de configuração do sequelize
    'models-path': resolve( __dirname, 'src', 'app', 'models'),  //Indicando o local dos models do sequelize
    'migrations-path': resolve( __dirname, 'src', 'database', 'migrations')  //Indicando o local das migrations do sequelize
}